name: Update controls_Pool.txt

on:
  push:
    paths:
      - 'FHEM/98_PoolControl.pm'
  workflow_dispatch:

jobs:
  update-controls:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4

      - name: Update controls_Pool.txt
        run: |
          SIZE=$(wc -c < FHEM/98_PoolControl.pm)
          DATE=$(date -u +"%Y-%m-%d_%H:%M:%S")
          echo "UPD ${DATE} ${SIZE} FHEM/98_PoolControl.pm" > controls_Pool.txt

      - name: Commit updated controls_Pool.txt
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add controls_Pool.txt
          git diff --cached --quiet || git commit -m "Auto-update controls_Pool.txt"
          git push
